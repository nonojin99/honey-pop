# 개발 일지 — 허니 팝

## 2026-09-05 · 착수부터 웹 빌드까지

Puzzle Bobble 계열을 Godot 4.7 로. 목표 플랫폼은 웹(HTML5) + 안드로이드, 모드는 싱글과
온라인 대결(1v1 / 2v2).

### 기획 의도

기존 13종과 중복 3축을 비교했다 — 입력(조준·발사) / 화면(고정 화면 + 상단 덩어리) /
실패 원인(하강 도달) 셋 다 신규다. 감성 스킨은 **육각 격자가 곧 벌집**이라는 점에서 나왔다.
장르 뼈대(검증된 Puzzle Bobble) + 독자적 스킨(아기 벌·벌집·꿀)이라는 성공 공식 그대로다.

2축: **조준 정밀도(축1) × 꽃가루 게이지(축2)**. 조준선은 벽 반사까지 그려 주는 축1의
도구인데, 조준하는 동안 꽃가루가 흐른다 — 도구의 값을 다른 축으로 치른다.

### 공정성 설계 (수식 먼저, 코드 나중)

`scripts/difficulty.gd` 가 유일한 원천. 전부 `하한 + 여유 × 감쇠^n`.

- `t_budget(n) = 2.5 + 6.5 × 0.90^n` — 가득 찬 게이지로 조준만 하며 버티는 시간.
  도입부 9.0s = 하한의 **3.6배** (권장 3~4배)
- `drain_rate(n) = 100 / t_budget(n) × escalation(라운드 내 발사 수)`
- `escalation(k) = min(2.0, 1 + 0.012k)` — 라운드 내부 압박 상승
- `colors(n) = min(6, 3 + n/3)`, `start_rows(n) = min(7, 2 + n/2)`
- `shots_per_descend(n) = 9 + 17 × 0.92^n`
- `round_quota(n) = 22 + 5n` — 이만큼 없애면 다음 라운드

공정성 규칙 4가지(`level.gd`):
1. 다음 구슬 색은 **판에 있는 색 중에서만** 뽑는다 (구조적 매치 불가능 발 금지)
2. 초기 배치의 모든 색은 3개 이상 (못 지우는 찌꺼기 색 금지)
3. 밀어내는 새 줄도 같은 규칙
4. 최하단+1 행(반드시 닿는 행)에 3개 매치가 나는 자리가 항상 존재하도록 보정

### 검증 결과

**① 수학적 공정성 — PASS** (판 600개 / 구슬 29,160개 / 각도 34,033회 스캔, 15초)
최소 매치만 성공시키는 플레이어의 조준 여유가 **압박 최대 상태에서도 0.30s ≥ 인간 하한 0.25s**.
전 구간(라운드 0~80) 클리어 불가능 상태 없음.

여기서 실제로 두 개를 잡았다:
- 궤적 종료 조건을 판 높이로 잡아 **모든 각도가 "놓을 수 없음"** 이 됐다. 발사대는 판보다
  아래에 있어서 첫 스텝에 바로 끊겼다. 기준을 발사 지점으로 바꿔 해결.
- 600판 중 2판이 **어떤 색·각도로도 3개 매치가 안 나는 막힌 판**이었다. 판 생성에 보정을 넣었다.
- 검증 도구 자체가 **거짓 PASS** 를 냈다(스크립트 오류로 D~H 가 통째로 건너뛰었는데 PASS).
  "검사가 실제로 돌았는가"를 검사 항목으로 승격시켰다.

**② 봇 시뮬레이션 — PASS** (봇 4종 × 시드 30, 4차 시도에서 통과)

| 판정 | 1차 | 최종 | 기준 |
|---|---|---|---|
| 초보 도입부 | 28% | **97%** (중앙 라운드 3) | 통과 가능 |
| 고수 무한생존 | 1판 | **0판** | 0판 |
| 변별력 고수/중수 | 1.47 | **1.66배** | 1.4배 |
| 변별력 중수/초보 | 4.40 | **1.44배** | 1.4배 |
| **축2 가치** | 1.43 | **2.55배** | 1.5배 |

최종 중앙값: SKILLED 5794(라운드 6) · NORMAL 3492(4) · NOVICE 2427(3) · AXIS1_ONLY 2273(3)

**하네스가 잡아낸 설계 결함 두 개** — 이게 봇 시뮬을 돌린 이유 전부다:

1. **발사 카운터가 축2를 희석시켰다.** 꽃가루를 관리하는 봇과 무시하는 봇의 하강 횟수가
   24.6 vs 24.5 로 사실상 같았다. 고갈은 6.0 vs 16.9 로 3배 차이가 나는데도. 하강의 76%를
   두 축 어디에도 안 붙은 맹목적 카운터가 만들고 있었다. 카운터를 12→26 발로 늦춰
   느린 안전장치로 물리자 축2 가치가 1.43 → 1.89배.
2. **난이도가 `round_no` 에만 걸려 있어 플래토가 생겼다.** 라운드는 판을 다 비워야 오르는데,
   하강이 9발마다 9개를 더하고 좋은 한 발이 3~8개를 지우니 판이 사실상 안 비워진다.
   실제로 봇 4종이 **전부 라운드 3에서 멈췄고** 고수는 400발까지 안 죽었다. 그러면 라운드별
   색 추가라는 진행 자체가 플레이어에게 영영 안 보인다. **할당량 진행**(`round_quota`)과
   **라운드 내부 압박 상승**(`escalation`)을 넣어 해결 → 무한생존 0판, 축2 가치 2.55배.

낙하 점수를 선형에서 초선형(`12 × d^1.45`)으로 바꾼 것도 여기서 나왔다 — 선형일 때
고수/중수가 1.14배로 붙었다. 실력이 점수로 벌어지는 지점이 필요했다.

**③ 시각 검증 — 통과** (`.shots/` 5장을 직접 눈으로 확인)
첫 판정은 **미달**이었다. 메뉴가 검은 여백에 글자만 얹힌 화면이라 감성 스킨(아기 벌)이 첫
화면에 전혀 없었다. 마스코트를 제목 위에 세우고 메뉴 뒤에 실제 판을 깔았다. "위험선" 라벨이
화면 오른쪽에서 잘리던 것도 함께 고쳤다.
이후 사용자 요청으로 구슬 안쪽 도형 무늬를 빼고 **명암(구형 셰이딩)** 으로 바꿨고, 벌도 같은
방식으로 입체화했다. 벌 날개는 정원 → 기운 타원 → 납작한 타원으로 두 번 고쳤다
(세로로 서면 토끼 귀처럼 보인다).

**④ 대결 통합 테스트 — PASS** (실제 Supabase Realtime 에 헤드리스 피어를 띄워 검증)
- 1 VS 1: 매칭 성립 → 양쪽이 같은 시드·같은 방을 독립 계산 → 공격 교환 → **A 승 / B 패**
- 2 VS 2: 4명이 한 방에, 팀 0/0/1/1 → **팀 1 승 · 팀 0 패로 전원 일치**.
  최고점(7550)을 낸 B 가 팀원 D 의 붕괴로 패배 — 팀 판정이 의도대로 동작한다

여기서 잡은 버그 세 개:
1. **seek 되인사 무한 루프** — 메시지를 받을 때마다 다시 인사하며 정착 타이머를 0 으로
   되돌려서, 두 피어가 서로를 보면서도 45초 동안 매칭이 성립하지 않았다.
   처음 보는 상대에게만 되인사하도록 수정 (net.gd 주석이 정확히 경고하던 함정이었다).
2. **로비 → 방으로 옮길 때 신원이 바뀌었다** — `connect_room` 이 매번 새 id 를 발급해
   로비에서 정한 명단의 키가 방에서 전부 어긋났고, 상대의 "탈락" 신호를 못 알아봐
   승패 판정이 아예 안 났다. 신원을 이어가는 인자를 추가.
3. **테스트 하네스 자체의 버그** — GDScript 람다는 지역 변수를 값으로 캡처한다.
   결과가 난 뒤에도 "시간 초과"가 찍혔다. 참조 타입으로 넘겨 해결.

### 이슈 · 남은 것

- **웹 빌드 용량** — wasm 38MB(gzip 9.7MB) + pck 1.3MB. 첫 로딩 약 11MB 는 Godot 웹의 고정
  비용이다. 하이퍼캐주얼 기준으로는 무겁다
- **기존 배포 채널과 안 맞는다** — Supabase `games` 테이블 → Render 는 단일 HTML 문서를
  서빙하는 구조라 다중 파일 40MB 빌드를 못 받는다. 정적 사이트 호스팅이 따로 필요
- **웹 터치 조작 미검증** — 엔진 부팅·렌더·콘솔 무에러까지는 봤지만 실제 드래그 조준은
  작업 환경의 브라우저 페인 제약으로 못 눌러 봤다. 실기기 확인 필요
- **APK 미빌드** — 이 PC 에 Android SDK/JDK 가 없다. 프리셋만 작성
- 공정성 여유가 1.2배로 빠듯하다(0.30s vs 0.25s). 압박 상한(`ESCALATE_MAX`)을 1.8 로 낮추면
  1.33배가 된다 — 실플레이 체감을 보고 판단할 것

### 다음엔 이걸 해볼 것

봇 시뮬에 **"판을 읽는 봇 vs 안 읽는 봇"** 을 추가해 조준선(축1 도구)의 가치를 재 보자.
지금 하네스는 축2의 가치만 잰다. 조준선을 없앤 봇과의 점수 차이가 작다면, 조준선이 축1을
너무 쉽게 만들어 정밀도가 실력이 아니게 됐다는 뜻이다.
